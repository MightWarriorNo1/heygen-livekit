// Configuration for the voice chat app
export const config = {
  avatarId: process.env.NEXT_PUBLIC_AVATAR_ID,
  voiceId: process.env.NEXT_PUBLIC_VOICE_ID,
  baseApiUrl: process.env.NEXT_PUBLIC_BASE_API_URL,
};
